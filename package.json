{
  "name": "@lyra-grants/arb-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "yarn workspace @lyrafinance/lyra-js build && yarn workspace @lyra-grants/arb-bot build",
    "start": "yarn workspace @lyra-grants/arb-bot start"
  },
  "workspaces": [
    "app",
    "sdk"
  ]
}
